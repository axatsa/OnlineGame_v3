# Проект "ClassPlay" (Online Game v3)

## 📝 Описание проекта
ClassPlay — это многофункциональная платформа для учителей и образовательных учреждений, предназначенная для геймификации обучения и создания учебных материалов с помощью ИИ. Проект объединяет в себе инструменты для создания квизов, рабочих листов, управления классами и интерактивных игр.

## 🛠 Технологический стек
- **Frontend:** React + Vite, TypeScript, Tailwind CSS, Shadcn UI.
- **Backend:** FastAPI (Python), SQLAlchemy, PostgreSQL (через Docker).
- **AI Интеграции:** OpenAI (GPT-4o), Google Gemini.
- **Инструменты:** Docker, Docker Compose.

## 🚀 Текущий функционал

### 1. Генераторы (Generator)
- **Quiz Generator:** Создание квизов по любой теме. Поддерживает экспорт в многостраничный PDF (10 вопросов на страницу + страница ответов).
- **Math Worksheet:** Генерация математических рабочих листов (до 30 задач на страницу).
- **Собственные инструменты:** Поддержка различных форматов учебных материалов.

### 2. Библиотека (Library)
- Хранение и управление созданными материалами.
- Автоматическая генерация названий и тем с помощью ИИ.
- Интерфейс для редактирования и предпросмотра контента.

### 3. Управление классами (Class Manager)
- Создание и управление списками учеников.
- Отслеживание прогресса и назначение заданий.

### 4. Игры (Games Library)
- **Tug of War:** Интерактивная игра с музыкальным сопровождением.
- Интеграция игровых механик в учебный процесс.

### 5. Панель Администратора
- Управление пользователями, ресурсами и системными настройками.

## 📂 Организация кода

### Backend (`/backend`)
- `routes/`: API эндпоинты (auth, classes, generator, admin, library).
- `services/`: Бизнес-логика и интеграции с ИИ (Gemini, OpenAI).
- `models.py`: Схемы базы данных.
- `schemas.py`: Pydantic модели для валидации данных.

### Frontend (`/front/src`)
- `pages/`: Основные страницы приложения.
- `components/`: UI компоненты (на данный момент требуют реструктуризации).
- `context/`: Управление состоянием (Authcontext и др.).

## 💡 Предложения по улучшению и оптимизации

### Архитектура
1.  **Реструктуризация Frontend:**
    - Разделение компонентов на `common` (кнопки, инпуты), `layout` (навигация, футер) и `features` (логика конкретных функций).
    - Вынос логики запросов в папку `api/`.
2.  **Backend Refactoring:**
    - Группировка утилит для работы с файлами и PDF в `utils/`.
    - Дальнейшее разделение `main.py` на более мелкие модули конфигурации.

### Функционал
- **AI Feedback:** Добавить возможность обратной связи от ИИ для учеников после прохождения квизов.
- **Интерактивные Доски:** Добавление модуля для совместной работы в реальном времени.
- **Многоязычность:** Поддержка нескольких языков (i18next).
- **Темная тема:** Полная адаптация всех компонентов под темный режим.

### DevOps
- Автоматизация деплоя (CI/CD).
- Улучшение логирования и мониторинга ошибок в реальном времени.
